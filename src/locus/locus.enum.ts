export enum SideLoading {
  LOCUSMEMBERS = 'locusMembers',
}

export enum Roles {
  ADMIN = 'admin',
  NORMAL = 'normal',
  LIMITED = 'limited',
}

export enum SortingOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
